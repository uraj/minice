1、is_assign判断赋值语句是否生成，如果生成，需要prepare。prepare当中，要判断要定值的寄存器对应的内容，是否是要定值的变量。
2、最开始，要把所有v_in[0]的活跃变量都load到寄存器之中。
3、var_map时，标记所有可能是结束块的块， 并返回所有可能被定值的全局变量。
4、将所有3得到的全局变量，加到3得到的块的v_out里面。
5、指针引用定值还是要flush所有pointer_entity，只是不判断dirty了。
6、所有关于dirty的信息，一律删除。
